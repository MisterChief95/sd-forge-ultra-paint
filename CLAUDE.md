# Claude Code guidance

See `AGENTS.md` for the agent guide for this repository (architecture,
conventions, validation commands, working style). Keep both in sync if either
changes — `AGENTS.md` is the source of truth.
